[package]
edition = "2021"
name = "ez-examples"
publish = false
version = "0.0.0"

[dependencies]
ez = { path = "../ez" }

[dev-dependencies]
tokio = { version = "*", features = ["full"] }

[package.metadata.release]
release = false
